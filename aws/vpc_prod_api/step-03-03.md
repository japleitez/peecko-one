#### Step 3.3: Store DB Credentials in Secrets Manager

aws secretsmanager create-secret --name prod/api-db-credentials --description "PostgreSQL prod peecko database credentials" --secret-string file://secret_prod_db.json
