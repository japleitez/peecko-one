# Create DB Subnet Group

# this part did not work --tag-specifications 'ResourceType:DBSubnetGroup,Tags=[{Key=Name,Value=prod-peecko-db-sg}]'

# so we need to create the Tag in AWS Console

aws rds create-db-subnet-group --db-subnet-group-name prod-peecko-db-subnet-group --db-subnet-group-description "Production Peecko Subnet group for RDS" --subnet-ids "$env:DB_SUBNET_1A" "$env:DB_SUBNET_1B"
