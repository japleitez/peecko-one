# ALB Security Group

aws ec2 create-security-group --group-name alb-sg --description "Security group for ALB" --vpc-id "$env:VPC_ID" --tag-specifications 'ResourceType=security-group,Tags=[{Key=Name,Value=alb-peecko-sg}]'
$env:ALB_SG="sg-0b9932c81673d5a78"

# EC2 Security Group

aws ec2 create-security-group --group-name app-sg --description "Security group for EC2 instances" --vpc-id "$env:VPC_ID" --tag-specifications 'ResourceType=security-group,Tags=[{Key=Name,Value=app-peecko-sg}]'
$env:APP_SG="sg-0532f38eb63092e6d"

# RDS Security Group

aws ec2 create-security-group --group-name rds-sg --description "Security group for RDS" --vpc-id "$env:VPC_ID" --tag-specifications 'ResourceType=security-group,Tags=[{Key=Name,Value=rds-peecko-sg}]'
$env:RDS_SG="sg-0360a3db428decfd7"
